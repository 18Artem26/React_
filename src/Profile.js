import React from 'react';

const Profile = () => {
  return (
    <div>
      <h2>Profile Page</h2>
      <p>This is a protected profile page, accessible only after authentication.</p>
    </div>
  );
};

export default Profile;
